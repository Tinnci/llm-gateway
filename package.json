{
  "private": true,
  "type": "module",
  "scripts": {
    "build:panel": "bun build --target=browser custom_components/llm_gateway/frontend/voice-harness-panel.js --outfile=/tmp/voice-harness-panel.js",
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260618.1"
  }
}
